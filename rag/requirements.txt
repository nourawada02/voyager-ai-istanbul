# Manual QA remediation Q.1 (§A): pinned exactly to the same versions
# already proven against the real Qdrant server and the real pinned
# embedding model during this remediation. torch is installed separately,
# first, from the CPU-only wheel index (see rag/Dockerfile) -- never
# listed here, for the same reason documented in
# services/istanbul-expert-b/Dockerfile (the default PyPI index pulls in
# the CUDA-bundled build, unused and unwanted for this CPU-only service).
qdrant-client==1.15.1
sentence-transformers==5.1.2
transformers==4.57.6
rank_bm25==0.2.2
