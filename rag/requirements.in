# Canonical runtime dependency source for the root-owned rag/ package
# (Checkpoint Phase 3, docs/adr/0005-phase3-rag-ownership.md). Mirrors the
# services/travel-mcp requirements.in/-lock convention (ADR 0002 §8) at
# root level, since rag/ has no owning submodule yet.
qdrant-client[fastembed]==1.15.1
sentence-transformers==5.1.2
transformers==4.57.6
torch==2.13.0
rank-bm25==0.2.2
pandas==3.0.5
numpy==2.5.2
