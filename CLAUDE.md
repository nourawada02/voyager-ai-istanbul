# VoyagerAI Istanbul

Decision-support system for trip planning to Istanbul: ranked flights, ranked stays with an explainable fair-price deal score, a side-aware and ferry-aware day-by-day itinerary, and a deterministic budget breakdown, all with full data-provenance. It never books, reserves, or pays for anything. Full spec: [`docs/architecture.md`](docs/architecture.md).

## Repository / submodule boundaries

This root repo is an orchestrator. Each `services/*` directory is an **independent git submodule with its own remote and commit history**:

| Submodule | Architecture role | Stack |
|---|---|---|
| `services/planner-a` | `agent-system-a` | LangGraph, FastAPI, SSE |
| `services/istanbul-expert-b` | `agent-system-b` | Google ADK, FastAPI, A2A |
| `services/travel-mcp` | `mcp-server` | Official MCP Python SDK, Streamable HTTP |
| `services/frontend` | `chatbot-ui` | Streamlit |
| *(no submodule — managed image)* | `vector-db` | Qdrant |

A change inside a submodule and the corresponding pointer bump in this root repo are **two separate commits, in two separate repos**. Committing a submodule pointer bump here does not commit whatever you changed inside that submodule, and vice versa.

## Cross-system invariants

- A2A is used only across the System A / System B network boundary — never between LangGraph nodes or plain function calls. ([architecture.md §4.2](docs/architecture.md#42-design-principles))
- MCP is used only for shared tools reachable over the network. ([§4.2](docs/architecture.md#42-design-principles))
- RAG (Qdrant) holds only stable knowledge; live/dynamic facts always go through MCP, never retrieval. ([§9.1](docs/architecture.md#91-corpus-boundary))
- Money is `Decimal` or integer minor units — never binary float. ([§12](docs/architecture.md#12-budget--currency-engine))
- No stack traces, exception text, file paths, or credentials ever reach a caller; full detail is logged server-side keyed by `trace_id`. ([§13.4](docs/architecture.md#134-error-handling--no-internal-detail-ever-reaches-the-caller))
- The system never books, reserves, or pays for anything, and never claims a fixture/historical listing is currently available. ([§1](docs/architecture.md#1-executive-summary), [§13.2](docs/architecture.md#132-output-guard))
- Every MCP tool / provider-adapter response carries the `data_mode`/provenance envelope. ([§8.2](docs/architecture.md#82-required-response-envelope)) A2A requests and exchanges separately carry their own required fields — `session_id`/`trace_id`/contract version on `LocalPlanRequest`, `task_id`/`context_id`/protocol version/artifact schema version on the exchange. ([§7.1–7.3](docs/architecture.md#7-agent-to-agent-a2a-collaboration-contract)) These are two distinct requirements — do not merge them into one universal envelope.

## Phase-gating rules

One gated phase at a time. No whole-repository code generation in a single pass. Each phase report lists files changed, commands run, test results, unresolved risks, and the next gate. A broken gate is reported, never hidden or worked around silently. Full phase table: [§17](docs/architecture.md#17-implementation-timeline--gated-phases).

**Current state as of 2026-08-17: Phases 0–3 are complete and frozen. Phase 0 (compatibility spike) closed. Phase 1 (contracts & fixtures) closed. Phase 2 (data & accommodation ML) is complete — Checkpoint A (data and architecture foundation), Checkpoint B (model experimentation and artifact selection), Checkpoint C.0 (accommodation-serving contract and safety design), Checkpoint C.1 (trusted resource loading and pure in-process accommodation-search service), and Checkpoint C.2 (production MCP exposure) are all complete. `search_stays` and `estimate_fair_price` are both real, registered production MCP tools, served over Streamable HTTP, backed by the frozen Checkpoint B model. Hotel-room and Shared-room listings remain unsupported by the V1 serving contract; every result carries explicit historical-snapshot, not-live-availability provenance. Phase 3 (multilingual RAG implementation and retrieval-configuration selection) is complete and frozen — see [ADR 0005](docs/adr/0005-phase3-rag-ownership.md) and [ADR 0006](docs/adr/0006-phase3-generation-acceptance.md). The frozen retrieval winner (dense config B) and its measured results are accepted. The strict 45-case generation acceptance report is retained as-is with one diagnosed judge-context false negative (`gt_03_en`) — `gate_passed: false` in that report is not hidden or reinterpreted as a pass; the citation-first judge-context repair is explicitly deferred past the Phase 4–6 critical path.

Phase 4 (System B / A2A vertical slice) is in progress and **not** closed. Checkpoint A — a deterministic side-aware, ferry-aware itinerary engine and stay-accessibility scorer in `services/istanbul-expert-b`, exposed as a genuine FastAPI app with the official A2A app mounted at `/`, verified with a real separate-process A2A task and an independent Docker build — is complete; see [ADR 0007](docs/adr/0007-phase4-checkpoint-a-system-b-a2a.md). Checkpoint B — operational, read-only Qdrant RAG integration into System B via a thin query-time adapter (`phase4/knowledge/`) that reproduces the frozen Phase 3 embedding/retrieval contract without importing the root `rag` package at runtime or performing any ingestion — is complete; see [ADR 0008](docs/adr/0008-phase4-checkpoint-b-rag-integration.md). Verified against a real Qdrant server populated by the frozen Phase 3 ingestion pipeline: citations flow through both the direct FastAPI and A2A paths in EN/TR/AR, and the service degrades cleanly to catalog-only scheduling with explicit warnings when Qdrant is unavailable. Inside the container, System B still has no access to the root-owned `rag/` package by design; RAG is optional at request time (`RAG_REQUIRED=false` by default). Checkpoint C.0 — the provider-neutral contract and architecture foundation for live weather, web-search evidence, and flight search, plus the bounded-ReAct orchestration design System A will later use to select among them — is complete and accepted; see [ADR 0009](docs/adr/0009-phase4-checkpointc0-live-data-and-react-design.md), which formally supersedes only the `search_flights`/`get_weather` rows of architecture.md §8. Checkpoint C.1 — a real, live Open-Meteo weather adapter (`providers/weather_openmeteo.py`) implementing the `WeatherProvider` interface, requiring no API key/account/billing — is complete; see [ADR 0010](docs/adr/0010-phase4-checkpointc1-openmeteo-weather-adapter.md). Verified against the real Open-Meteo API (geocoding + current-conditions) for Istanbul: correct timezone-aware timestamps, CC BY 4.0 attribution, and full two-stage schema validation, with an honest `cache_status="bypass"` since no cache is implemented. Checkpoint C.2 — a real SerpApi (Google Search) web-evidence adapter (`providers/web_evidence_serpapi.py`) implementing the `WebEvidenceProvider` interface — is complete; see [ADR 0011](docs/adr/0011-phase4-checkpointc2-serpapi-web-evidence-adapter.md), which also documents an earlier, abandoned Tavily-based draft of this checkpoint (no Tavily code was ever committed) pivoted to SerpApi after Tavily's website and keyless API proved unreachable from this network, and a timeout-semantics repair to `providers/http_transport.py` (the stdlib `urllib.request` enforces only a single end-to-end socket timeout, not a separate connect-phase bound; the shared transport previously passed the wrong tuple element to it). Verified against the real SerpApi Google-engine endpoint for an Istanbul query: real organic-result evidence, correct provider identity, full two-stage schema validation, deterministic host-based source classification, and secret isolation (the `api_key` query parameter never enters a log, fingerprint, or envelope). Checkpoint C.3 — a real SerpApi Google Flights adapter (`providers/flights_serpapi.py`) implementing the `FlightSearchProvider` interface, exact-date one-way search only — is complete; see [ADR 0012](docs/adr/0012-phase4-checkpointc3-serpapi-google-flights-adapter.md). Verified against the real SerpApi Google Flights endpoint for a Beirut–Istanbul search: real itinerary snapshots, correct provider identity, full two-stage schema validation (envelope, result, and every individual `FlightOption`), offset-aware timestamps via an injectable IATA-to-timezone resolver, `Decimal`-exact TRY price conversion, and the same secret-isolation channel established in Checkpoint C.2. Round-trip, multi-city, booking, payment, and MCP exposure of flight search are all explicitly deferred. Checkpoint D.0 — System A's bounded LangGraph ReAct control loop and a real Qwen structured-decision provider, with deterministic fake tool executors for every allowed capability (`services/planner-a/phase4/`) — is complete; see [ADR 0013](docs/adr/0013-phase4-checkpoint-d0-system-a-react-core.md). A genuine compiled `StateGraph` (InputGuard/LoadSession/Decide/Execute/Observe/Update/Synthesize/Degrade/End) enforces recursion_limit=25, an 8-call external-tool budget, a 2-call-per-tool cap, 2 decision-format repairs, a 60s deadline, duplicate-action fingerprinting, and cancellation checks in code, not just prompts; Qwen may only select from a closed action allowlist with per-action closed argument schemas. Verified against the real Qwen Chat Completions endpoint: one structured `search_flights` decision for a Beirut–Istanbul request, schema-valid, no chain-of-thought, no credential exposure. Checkpoint D.1 — the production composition layer (root-owned `orchestration/system_a/`) that injects real capabilities into System A's bounded loop in place of the fake tool executors — is complete; see [ADR 0014](docs/adr/0014-phase4-checkpoint-d1-system-a-real-wiring.md). `get_weather`/`web_search`/`search_flights` bind directly to the real root providers; `search_stays`/`estimate_fair_price` bind to Travel MCP over real Streamable HTTP; `call_istanbul_expert` binds to System B over real A2A, reusing the exact Phase 0 client sequence. planner-a gained a small additive `ExecutionContext` (`ToolExecutor.execute()`'s new optional third parameter, backward compatible with `FakeToolExecutor`) and three contract-audit fixes (`ProviderResponseEnvelope`/`FlightOption` 1.1.0-additive fields; Travel-MCP/System-B results are not `ProviderResponseEnvelope`-wrapped, unlike the three root-provider actions) found while wiring real data through validation that had only ever been exercised against this checkpoint's own fake fixtures. The canonical five-tool-call plan measured 26 real LangGraph transitions against the fixed 25-transition ceiling; Observe and Update now share one physical step for a genuine tool execution (the `update` node itself is unchanged, still used for the duplicate-skip path), bringing the same plan to 20 transitions with no bound loosened. A real, bounded, cross-process gate (real Travel MCP and System B processes, real Open-Meteo call, scripted, not live, decisions) proved genuine live weather, Travel MCP, and System B A2A responses against the real ~23.5k-row Istanbul dataset — `call_istanbul_expert` returns a real, schema-valid `LocalItinerary` through the official A2A transport with `status="success"` in ~13s. (Checkpoint D.1.1: the first cross-process attempt's real A2A task appeared to hang past every timeout tried; root-caused to an undrained `subprocess.PIPE` deadlock in the test harness itself, never a System B or A2A-client defect — repaired by redirecting subprocess output to a log file, matching System B's own already-proven test pattern; see ADR 0014 §10.1.) Checkpoint D.2A — the public System A API/SSE surface and SQLite persistence — is complete; see [ADR 0015](docs/adr/0015-phase4-checkpoint-d2a-system-a-api.md). A real FastAPI service (`orchestration/system_a/api.py`, `create_app()` factory) exposes `GET /health`, `POST /v1/runs`, `GET /v1/runs/{id}`, `GET /v1/runs/{id}/events` (SSE), and `POST /v1/runs/{id}/cancel`, backed by `RunStore` (SQLite, WAL + bounded busy_timeout) for run/session/event persistence and a real `langgraph-checkpoint-sqlite` `SqliteSaver` in place of `MemorySaver` for the graph's own checkpoint state. A new root `pyproject.toml` (editable-installable, mapping `phase1`/`phase4` at `services/planner-a`'s own directories via `package_dir`) replaces Checkpoint D.1's temporary `PYTHONPATH` composition convention with a real declared dependency path for this service, with no change to `services/planner-a`'s own independent buildability. Every D.0/D.1 bound (recursion limit 25, 8-call/2-per-tool budgets, 2 decision repairs, 60s deadline, duplicate-action detection, cancellation checks, the closed action allowlist) is preserved unmodified; run execution happens on a bounded real-thread pool, never the FastAPI event loop. `phase1.models.StreamStage`/`contracts/StreamEvent.schema.json` gained eight additive 1.1.0 stage values (`run_started`/`action_started`/`action_completed`/`action_failed`/`run_completed`/`run_degraded`/`run_failed`/`run_cancelled`) for the ReAct action-loop's own SSE vocabulary, alongside (never replacing) the original architecture.md §15.4 linear-pipeline vocabulary. Checkpoint D.2B — the real Streamlit frontend, an explicit deterministic demo/fixture mode, and a five-service root Docker Compose stack — is complete; see [ADR 0016](docs/adr/0016-phase4-checkpoint-d2b-frontend-and-compose.md). `services/frontend/phase6/` (`api_client.py`, `results.py`, `app.py`) is the real `chatbot-ui`, talking only to `agent-system-a` over HTTP/SSE — never to providers, Travel MCP, Qdrant, or System B directly. `orchestration/system_a/config.py::system_a_mode()` adds an explicit `VOYAGER_SYSTEM_A_MODE` switch (`"real"` default, or exactly `"fixture"` for the new `FixtureDecisionProvider` + the already-existing `phase4.tools.FakeToolExecutor`, network-free) that never silently falls back in either direction; `/health` exposes the resulting non-secret `mode` for the UI to label. Root `docker-compose.yml` brings up exactly five services (`agent-system-a`, `agent-system-b`, `mcp-server`, `vector-db`, `chatbot-ui`) wired only by internal Compose service names, with named volumes for System A's SQLite store and Qdrant storage; `agent-system-a` and `mcp-server` now build from the repository root so they can install the real `orchestration`/`providers` packages and the real accommodation dataset/ML bundle respectively, never a PYTHONPATH/sys.path hack. Verified end-to-end in real Docker containers, fixture mode, zero paid-provider calls: all five containers reach `healthy`; a real trip submitted through `POST /v1/runs` streams real sanitized SSE progress and reaches a terminal, schema-valid `completed` result; that result survives `docker compose restart agent-system-a`; `docker compose down` stops all five cleanly with no sixth container ever observed. `QDRANT_URL` defaults to empty (System B degrades to catalog-only via the existing `RAG_REQUIRED=false`) rather than always-on, after an always-on default was found during this checkpoint's own gate to force a multi-minute embedding-model cold start on every fixture-mode boot. The final README/EVALUATION rewrite and the formal Phase 6 evaluation suite remain explicitly deferred past this checkpoint — Phase 4's construction work is now complete. Checkpoint D.3 — an internal Travel Search ReAct specialist inside System A — is complete; see [ADR 0017](docs/adr/0017-phase4-checkpoint-d3-travel-search-specialist.md). System A now contains a bounded LangGraph ReAct supervisor and exactly one internal, compiled Travel Search ReAct specialist (`services/planner-a/phase4/specialist.py`, a genuine separate `StateGraph`, never a plain loop and never a second service). The specialist owns `get_weather`, `web_search`, `search_flights`, `search_stays`, and `estimate_fair_price`; the supervisor owns delegation (`call_travel_search`), `call_istanbul_expert` over real A2A, and synthesis — it can no longer call any of the five travel-search tools directly. Both agents have separate prompts, typed state, separately constructed `DecisionProvider` instances, and closed action allowlists, with role fixed by construction, never inferred from prompt text. The global 60s deadline, the 8-call external-tool budget, cancellation, and the duplicate-call fingerprint registry remain shared across both agents (one counter, one clock origin, one registry); the specialist additionally enforces its own smaller bounds (≤15 graph transitions, ≤5 real tool calls per delegation). The public System A API, the frontend result shape, and the five-service Compose topology are unchanged. A twelve-case fixture suite (`phase4/tests/test_d3_evaluation.py`) measures structural conformance of the graphs under scripted ground-truth decisions (delegation routing, tool-execution boundaries, schema validity, degradation handling) — it does not measure, and is not cited as measuring, live-Qwen intent recognition or routing accuracy, which remains part of this project's still-pending final evaluation phase. Verified live in real Docker containers, fixture mode, zero paid-provider calls: all five containers healthy; a real trip streamed honest, real-time per-action SSE progress through the specialist and the supervisor's own A2A call to System B; the completed run and every specialist-produced observation survived `docker compose restart agent-system-a`. An in-progress specialist delegation is not resumed mid-delegation after a process crash — it reconciles to `failed` via D.2A's existing `RunStore.reconcile_incomplete_runs_on_startup`, the same honest behavior any other interrupted run already receives; no new persistence system was built to narrow this gap.** Update this line only when a phase gate is formally passed.

## Testing / reporting expectations

Every later phase is measured against the targets in [§14.10](docs/architecture.md#1410-targets): routing accuracy ≥90%, tool-selection correctness ≥90%, schema validity 100%, budget arithmetic errors 0, hard itinerary violations 0, grounded-claim citation coverage 100%, external-call timeout test coverage 100%, ML MAE improvement over strong baseline ≥10% to enable a learned model.

## Operational warnings

- Submodule commits and root pointer-bump commits are never combined into one commit.
- Do not commit or push without explicit approval.
